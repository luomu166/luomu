# 个人作品集网站 · 使用文档

## 一、项目概览

这是一个为廖昌林搭建的个人作品集静态网站，无框架、无构建步骤，浏览器直接打开即可运行，也方便部署到任何静态托管平台。

设计方向：**暗色系、高级、克制、有科技感，不走模板路线**。整体为红黑配色——近黑底色 + 红色点缀，衬线中文大标题 + 等宽英文小标签，配合细线网格、噪点、粒子背景等细节。

页面结构（对应你的需求）：

| 模块 | 说明 |
| --- | --- |
| 1. 全屏 Hero | 视频背景（可替换）+ Canvas 粒子兜底、大标题、导航栏、联系按钮、滚动提示 |
| 2. 个人经历 | 肖像占位、自我介绍、联系方式、GPA/排名/活动/拉新数据、教育经历时间线、荣誉证书 |
| 3. 精选项目 | 6 张大卡片作品展示（汽车渲染 / 角色 IP / 品牌视觉 / 产品广告 / AI 概念 / CG 动画） |
| 4. 个人优势 | 6 张能力卡片（视觉设计 / AI 设计 / 品牌设计 / 汽车渲染 / 三维建模 / 视频后期） |
| 5. 联系 | 大号联系区块 + 电话 / 邮箱 / 百度网盘作品集链接 + 页脚 |

## 二、目录结构

```text
portfolio-site/
├─ index.html                    # 全部页面文案与结构
├─ assets/
│  ├─ css/style.css              # 全部样式，设计变量集中在文件顶部 :root
│  ├─ js/main.js                 # 交互脚本
│  ├─ img/
│  │  ├─ portrait.svg            # 头像占位图（可替换）
│  │  ├─ favicon.svg             # 站点图标
│  │  └─ projects/               # 11 张个人作品图（work-01.jpg ~ work-11.jpg）
│  └─ media/
│     └─ hero-poster.jpg         # Hero 视频封面（未放视频时也有效果）
└─ README.md
```

## 三、修改内容

所有文字均在 `index.html`，用编辑器搜索关键字即可定位。

### 1. Hero 首屏

- 大标题：搜索 `hero-title`，默认「让想象 / 拥有质感」。
- 身份标签：搜索 `hero-roles`，默认列出视觉设计师、AI 设计师、品牌设计师、汽车渲染师、建模师。
- 底部学校信息：搜索 `hero-meta`。

### 2. 个人经历

- 自我介绍：搜索 `about-copy`，两段文字按需修改。
- 项目数据：搜索 `about-stats`，四个数字（GPA 3.6、排名 3/16、活动 10+、拉新 50+）。数字有滚动计数动画，改 `data-count` 属性即可，带小数则加 `data-decimal="1"`。
- 联系方式：搜索 `about-contact`，修改电话、邮箱、作品集链接。
- 技能标签：搜索 `about-skills`。
- 教育 / 经历时间线：搜索 `timeline`。
- 荣誉证书：搜索 `honors`。

### 3. 精选项目

- 11 张卡片对应你的 11 件个人作品：前 5 张来自 `个人作品/`，后 6 张来自 `个人作品/作品1/`，已统一压缩为 1600×900 的 `work-01.jpg` ~ `work-11.jpg`。
- 每张卡片是一个 `<article class="work-card">`，包含封面、编号、标题、标签，文字直接改 `index.html`。
- 当前标题为「作品 01 ~ 05」，建议按每张图的实际内容改成具体名称（如「保时捷 911 渲染」「室内场景」「产品海报」）。
- 项目跳转链接默认指向百度网盘作品集，逐个搜索 `pan.baidu.com` 替换为各项目真实链接。
- 卡片布局：按「大卡 7 列 + 小卡 5 列」交替排列，最后一张为奇数时会自动变为通栏收官卡；全图按 16:9 居中裁切，逻辑在 `style.css` 的 `.works-grid`。

### 4. 个人优势

- 搜索 `strengths-grid`，每张卡片含图标（内联 SVG）、标题与描述，按需增删；图标可换成任意 24×24 的 SVG path。

### 5. 联系

- 搜索 `contact-inner` 与 `contact-links`，电话、邮箱、作品集链接都在这里。

## 四、替换图片与视频

### 头像

已替换为你的肖像照片（`大头照片.png` 居中裁切为 4:5，存为 `assets/img/portrait.jpg`）。想换照片：直接用同名 `portrait.jpg` 覆盖即可。若头部在画面中位置不居中，告诉我方向，我可以调整裁切偏移。

如需要重新走一遍替换流程：把新照片放到 `assets/img/`，推荐竖版 640×800 或 800×1000，并将 `index.html` 中 `portrait.jpg` 的引用保持同名即可。

### 项目作品图

11 张作品已放在 `assets/img/projects/`（`work-01.jpg` ~ `work-11.jpg`）。想换图：直接用同名新文件覆盖即可，无需改代码。

- 推荐尺寸：1600×900（16:9），居中裁切显示
- 如果想调整裁切位置，可以用图片工具把主体放在画面中央；或在 `style.css` 的 `.work-media` 修改 `object-position`（如 `left center` / `top`）

### Hero 视频

已内置你的 `11月1日.mp4`（已压缩为 1920×1080 / 30fps / 约 17MB，复制为 `assets/media/hero.mp4`），打开网站即自动循环播放，并叠加了胶片颗粒质感。想换视频，用同名文件覆盖即可。建议：

- 分辨率 1920×1080 或以上，时长 8–20 秒的无缝循环
- 编码 H.264，体积尽量控制在 20MB 内
- 画面中避免出现文字，方便叠加标题

注意：Cloudflare Workers 静态资源单文件上限为 25MiB，原片 26.4MB 会导致部署失败，新视频已压到 17MB，可直接部署。之后换视频时请保持单文件小于 25MiB；压缩可用剪映或 Blender（打开文件 → 渲染属性 → FFmpeg H.264）完成。

未放入视频时，网站自动显示 Canvas 粒子动态背景，不会空白。

## 五、部署上线

### 方案 A：GitHub Pages（免费）

1. 新建仓库，把 `portfolio-site` 整个文件夹推上去；
2. 仓库 Settings → Pages → Source 选择分支根目录；
3. 访问 `https://用户名.github.io/仓库名/`。

### 方案 B：Vercel / Netlify（免费，国内访问更快可选 Vercel）

1. 注册并导入仓库；
2. 框架预设选「Other」，构建命令留空，输出目录选 `portfolio-site`；
3. 每次推送自动部署。

### 方案 C：本地直接使用

双击 `index.html` 即可预览。注意：部分浏览器对 `file://` 下的自动播放有限制，本地预览建议起服务（见 README）。

## 六、自定义设计变量

`assets/css/style.css` 顶部 `:root` 定义了全套设计令牌，改一处全局生效：

| 变量 | 默认值 | 作用 |
| --- | --- | --- |
| `--bg` | `#0a0a0d` | 页面主背景 |
| `--text` | `#f0f0ec` | 主文字 |
| `--muted` | `#9b9ba6` | 次级文字 |
| `--accent` | `#ff3b3b` | 强调色（红） |
| `--violet` / `--cyan` | `#ff5252` / `#a31212` | 点缀光效色（红/深红） |
| `--font-serif` | Noto Serif SC | 中文大标题字体 |
| `--font-display` | Space Grotesk | 英文 / 数字字体 |

想换主题色，只改 `--accent` 即可；想换字体，改 `--font-serif` 与 `--font-display`。

## 七、常见问题

**字体加载慢或离线打不开？** 页面引用了 Google Fonts，加载失败会自动回退到系统字体（苹方 / 微软雅黑 / 宋体），不影响使用；若需完全离线，可下载字体文件放入 `assets/fonts/` 并用 `@font-face` 引入。

**视频不自动播放？** 浏览器策略要求静音循环视频才能自动播放，代码已设置 `muted loop playsinline`；另外 `file://` 直接打开时部分浏览器会拦截，请用本地服务或部署后访问。

**移动端表现？** 全站响应式：导航收起为全屏菜单，卡片单列堆叠，统计数字 2×2 排列，并兼容 `prefers-reduced-motion`（减少动效）。

**想加新项目？** 复制一个 `<article class="work-card">` 结构，修改图片、编号、标题、标签即可；超过 6 个时，把 `.work-card:nth-child(5)` 这类规则按新的布局调整即可。

## 八、技术栈

- 纯 HTML / CSS / JavaScript，零依赖，无构建步骤
- 原生 IntersectionObserver 实现滚动显现与数字计数
- Canvas 2D 实现 Hero 粒子背景（视频存在时自动让位）
- 语义化标签与无障碍支持（跳过链接、aria、键盘焦点、动效偏好）
